import localInnerClass.Outer;
import nestedClass.Car;
import org.junit.jupiter.api.Test;

public class LocalInnerClassTests {

    @Test
    void localInnerClassTest() {
        Outer outer = new Outer();
        outer.someMethod();//number + x = 16
    }
}
